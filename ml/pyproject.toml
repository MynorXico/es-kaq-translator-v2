[project]
name = "translator-ml"
version = "0.0.1"
description = "ML pipeline (data prep, training, evaluation) for the Spanish<->Kaqchikel translator"
requires-python = ">=3.11"
dependencies = [
    "sacrebleu>=2.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["evaluation"]

[tool.ruff]
line-length = 100
target-version = "py311"
